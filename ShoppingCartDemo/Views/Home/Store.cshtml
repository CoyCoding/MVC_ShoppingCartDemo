@using ShoppingCartDemo.Models.HtmlHelpers
@model ShoppingCartDemo.Models.ProductListViewModel

@{

    ViewBag.Title = "Shopping cart display";
    Layout = "~/views/shared/_Layout.cshtml";

}
<div class="container-fluid">
    <div class="text-center"><h2>@ViewBag.Title.</h2></div>

    <div class="row">
        <div id="CatSelection" class="col-3">
            Place Holder Text
        </div>
        <div class="col-9">
            @foreach (var product in Model.Products)
            {
                <div class="card p-4 m-4">

                    <h3>

                        <b class="">@String.Format(product.Name):</b>
                        <span class=" badge badge-info float-right">@String.Format("{0:C}", product.Price)</span>
                    </h3>
                    <span>@product.Description</span>

                </div>

            }
            <div class="btn-group-sm float-right p-2">
                @Html.PaginationLinks(Model.PagingInfo, pageNum => Url.Action("Store", new { page = pageNum }))
            </div>
        </div>
       
    </div>
</div>

@section Scripts{

}